import React from "react";
import { getWeatherInfo } from "../services/weatherInfo";

export const WeatherMainInfo = () => {
  const handleGetWeatherInfo = getWeatherInfo();

  return <div></div>;
};
